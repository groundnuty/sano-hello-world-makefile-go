name: Build and Deploy

on:
  push:
    branches:
      - main
  workflow_dispatch:
    inputs: {}

jobs:
  build:
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v3
      - name: Build the docker image
        run: make build